/**
 * DEMO_MODE Flag
 * 
 * When true: App uses fixed demo data for deterministic screenshots.
 * When false: App runs with normal production behavior.
 * 
 * Set to true ONLY for store screenshot captures.
 */
export const DEMO_MODE = true;
